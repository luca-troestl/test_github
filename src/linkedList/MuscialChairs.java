package linkedList;

import java.util.Arrays;
import java.util.List;

public class MuscialChairs {

	private List<String>names;
	
	public MusicalChairs(String...strings names) {
		if(names.length != 0) {
			this.names = new LinkedList<String>(Arrays.asList(names));
		}
	}
	


